# Only design-system specifics. Dependencies, .env files, OS cruft and editor
# state are ignored repo-wide by the root .gitignore.

# testing
/coverage

# production
/build

# storybook
storybook-static

# SCSS to CSS
/src/styles/css
