const auth = {
  "auth.cookie-name.id-token": "go-session:id_token",
  "auth.cookie-names.session-type": "go-session:type",
  // How long the CMS's answer about an Adgangsplatformen session is trusted
  // before we ask again.
  "auth.session-validation-ttl-seconds": 30,
}

export default auth
