# Claude instructions

This repo uses [`AGENTS.md`](./AGENTS.md) as the shared source of truth for
AI-assisted tools (Claude, Cursor, and others). Always read it first.

When working inside a subdirectory, also read that subdirectory's `AGENTS.md`
if one exists — it contains role and stack guidance specific to that project. 
Always read any relevant `AGENTS.md` files.
