# Semantic drift between schemas/openapi/publizon-adapter.yaml (canonical)
# and the former go/lib/rest/publizon/publizon-spec.yaml. Style noise
# (quotes, JSON spacing) normalized away first. Dominant drift: go's spec
# had `operationId:` fields on most endpoints; the canonical doesn't.
--- schemas/openapi/publizon-adapter.yaml (canonical, normalized)
+++ go/lib/rest/publizon/publizon-spec.yaml (former, normalized)
@@ -10,6 +10,7 @@
     get:
       tags:
         - CardNumber
+      operationId: getV1UserCardnumberFriendly
       summary: Gets the user's friendly card number.
       description: "Sample **request**:\r\n            \r\n    GET /friendly\r\n    {\r\n        \"name\": \"Some name\"\r\n    }"
       parameters:
@@ -79,6 +80,7 @@
     get:
       tags:
         - Checklist
+      operationId: getV1UserChecklist
       summary: List all checklist items of the user.
       parameters:
         - name: clientId
@@ -114,6 +116,7 @@
     post:
       tags:
         - Checklist
+      operationId: postV1UserChecklistIdentifier
       summary: Add a book to the user's checklist.
       parameters:
         - name: clientId
@@ -153,6 +156,7 @@
     delete:
       tags:
         - Checklist
+      operationId: deleteV1UserChecklistIdentifier
       summary: Delete a book from the user's checklist.
       parameters:
         - name: clientId
@@ -193,6 +197,7 @@
     get:
       tags:
         - Library
+      operationId: getV1LibraryProfile
       parameters:
         - name: clientId
           in: header
@@ -222,6 +227,7 @@
     get:
       tags:
         - Loan
+      operationId: getV1UserLoans
       summary: Lists all active loans of the user.
       parameters:
         - name: clientId
@@ -262,6 +268,7 @@
     get:
       tags:
         - Loan
+      operationId: getV1UserLoansIdentifier
       summary: Gets details of a specific loan.
       parameters:
         - name: clientId
@@ -303,6 +310,7 @@
     post:
       tags:
         - Loan
+      operationId: postV1UserLoansIdentifier
       summary: Creates a new loan.
       parameters:
         - name: clientId
@@ -391,6 +399,7 @@
     post:
       tags:
         - LoanStatus
+      operationId: postV1Loanstatus
       summary: Get loan status of all products.
       parameters:
         - name: clientId
@@ -443,6 +452,7 @@
     get:
       tags:
         - Product
+      operationId: getV1ProductsIdentifier
       summary: Get a specific product.
       parameters:
         - name: clientId
@@ -478,6 +488,7 @@
     get:
       tags:
         - Product
+      operationId: getV1Products
       summary: List all products.
       parameters:
         - name: clientId
@@ -508,6 +519,7 @@
     get:
       tags:
         - Reservation
+      operationId: getV1UserReservations
       summary: Lists all reservations of the user.
       parameters:
         - name: clientId
@@ -543,6 +555,7 @@
     post:
       tags:
         - Reservation
+      operationId: postV1UserReservationsIdentifier
       summary: Creates a new reservation.
       parameters:
         - name: clientId
@@ -593,6 +606,7 @@
     patch:
       tags:
         - Reservation
+      operationId: patchV1UserReservationsIdentifier
       summary: Updates the phone number and email address of a specific reservation.
       parameters:
         - name: clientId
@@ -643,6 +657,7 @@
     delete:
       tags:
         - Reservation
+      operationId: deleteV1UserReservationsIdentifier
       summary: Cancels a reservation.
       parameters:
         - name: clientId
